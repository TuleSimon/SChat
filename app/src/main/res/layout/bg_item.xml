<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="3dp"
    android:animationCache="true"
    android:backgroundTint="@color/colorPrimary"
    android:clickable="true"
    android:clipToPadding="true"
    android:focusable="auto"
    android:padding="3dp"
    app:cardPreventCornerOverlap="false"
    app:rippleColor="@color/colorPrimary">

    <ProgressBar
        android:id="@+id/bg_progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignStart="@id/bg_item_bg"
        android:layout_alignTop="@id/bg_item_bg"
        android:layout_alignEnd="@id/bg_item_bg"
        android:layout_alignBottom="@id/bg_item_bg"
        android:fitsSystemWindows="false"
        android:indeterminate="true"
        android:indeterminateOnly="true"
        android:indeterminateTint="@color/colorPrimary" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/bg_item_bg"
        android:layout_width="112dp"
        android:layout_height="92dp" />

</RelativeLayout>