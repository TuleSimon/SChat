<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whatsapclone">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity android:name=".activity.Reviewpost"></activity>
        <activity android:name=".activity.reviewActivity" android:launchMode="singleTop" />
        <activity android:name=".activity.createPost" android:launchMode="singleTop" />
        <activity
            android:name=".activity.messageActivity"
            android:parentActivityName=".activity.DashBoard"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".activity.viewProfileImage"
            android:parentActivityName=".activity.Settings"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.Settings"
            android:launchMode="singleTop"
            android:parentActivityName=".activity.DashBoard" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".activity.DashBoard"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.createAccount"
            android:launchMode="singleTop"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.loginActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".activity.MainActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>